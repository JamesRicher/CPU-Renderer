#include "mesh_parser.h"
#include "mesh.h"

Mesh MeshParser::loadFromObj(const char* obj_path) {
    Mesh mesh;

    return mesh;
}

bool MeshParser::saveAsJson(const Mesh& mesh, const char* json_path) {
    return false;
}

bool MeshParser::saveAsBinary(const Mesh& mesh, const char* binary_path) {
    return false;
}
